include ../blocks.project/includes

doctype html
html(lang="ru").html
  include templates/head/head
    block head
      - var pageTitle = "Фото работ";

  +page

    +header(false).__header


    +b.content.__content

      +font-switch

      .grid
        +breadcrumbs(["Главная", "Фотогалерея"])._top

      //-.grid.grid_no-sm-padding
        +section-gallery


    +footer-inner.__footer
