include ../blocks.project/includes

doctype html
html(lang="ru").html
  include templates/head/head
    block head
      - var pageTitle = "Контакты";

  +page

    +header(false).__header


    +b.content.__content

      +font-switch

      .grid
        +breadcrumbs(["Главная", "Наши контакты"])._top

      +section-contacts


    +footer-inner.__footer
