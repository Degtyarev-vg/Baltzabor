include ../blocks.project/includes

doctype html
html(lang="ru").html
  include templates/head/head
    block head
      - var pageTitle = "Статьи";

  +page

    +header(false).__header


    +b.content.__content

      +font-switch

      .grid
        +breadcrumbs(["Главная", "Скидки и акции"])._top

      +section-issues._item


    +footer-inner.__footer
